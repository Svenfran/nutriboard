<%= render 'shared/sidebar' %>
<div class="content">
  <div class="form-ncu">
    <h2 class="mb-3 text-center"><%= @meal.name.upcase %></h2>
    <p class="mb-3 text-center"><%= @meal.created_at.strftime("%b %d, %Y") %></p>
    <%= render 'shared/food-form' %>
  </div>
</div>